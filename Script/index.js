// let loader = document.getElementById("loader");
// let start = document.getElementById("start");
// let resume = document.getElementById("res");



// resume = () => {

// resume.removeChild("res");

// };
// praise = () => {
    // setTimeout(() => {
    // loader.classList.add("hidden");
    // start.classList.remove("hidden");
        // loader.classList.add("hidden");
    // start.classList.remove("hidden");
    // loader.classList.add("hidden");
    // }, 5000);
    // setTimeout(() => {
    //     start.classList.remove("hidden");
    //     loader.classList.add("hidden");
    // }, 1000);
    // start.classList.remove("hidden");
    // loader.classList.add("hidden");
// };
// setTimeout(() => {

// start.classList.remove("hidden");
// loader.classList.add("hidden");
// }, 1000000);